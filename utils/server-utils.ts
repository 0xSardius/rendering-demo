"server-only"

export const serverSideFunction = () => {
    console.log(
        `Use multiple libraries
        use environment variables,
        interact with a database,
        or process confidential data.`
    );
    return "Server-side result";
}
